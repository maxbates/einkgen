# Pillow is provided by the Klayers public layer (see infra/README.md).
# Only the bits not in the layer or the Python 3.12 runtime go here.
# boto3 is in the runtime — don't ship it.
openai>=1.0,<2.0
python-ulid>=3.0,<4.0
