#pragma once

// Copy this file to `secrets.h` (gitignored) and fill in real values.

#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"

#define MANIFEST_URL        "https://cdn.example.com/current/manifest.json"
#define DEVICE_STATUS_URL   "https://abc123.lambda-url.us-east-1.on.aws/"
#define DEVICE_STATUS_TOKEN "replace-me"

#define FW_VERSION "0.1.0"
